{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.10.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"nvidiaTeslaT4","dataSources":[{"sourceId":11160104,"sourceType":"datasetVersion","datasetId":6963602},{"sourceId":11171188,"sourceType":"datasetVersion","datasetId":6971656}],"dockerImageVersionId":30919,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"import sentencepiece as spm\n\n# Train SentencePiece tokenizer\nspm.SentencePieceTrainer.train(\n    input='/kaggle/input/datamalayalam/5000data', \n    model_prefix='malayalam_bpe', \n    vocab_size=5000, \n    model_type='bpe',  # Byte Pair Encoding\n    character_coverage=1.0\n)\n\n# Load the trained tokenizer\nsp = spm.SentencePieceProcessor(model_file='malayalam_bpe.model')\n\n# Example tokenization\ntext = \"മലയാളം നമുക്ക് അറിയാം\"\ntokens = sp.encode(text, out_type=str)\nprint(tokens)\n","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2025-03-26T08:51:11.869684Z","iopub.execute_input":"2025-03-26T08:51:11.870038Z","iopub.status.idle":"2025-03-26T08:51:13.861960Z","shell.execute_reply.started":"2025-03-26T08:51:11.870006Z","shell.execute_reply":"2025-03-26T08:51:13.861128Z"}},"outputs":[{"name":"stdout","text":"['▁മലയാളം', '▁ന', 'മു', 'ക്ക്', '▁അറിയ', 'ാം']\n","output_type":"stream"}],"execution_count":5},{"cell_type":"code","source":"import torch\nfrom torch.utils.data import Dataset, DataLoader\n\nclass MalayalamDataset(Dataset):\n    def __init__(self, file_path, tokenizer, context_length=256):\n        with open(file_path, 'r', encoding='utf-8') as f:\n            self.text = f.read()\n        self.tokenizer = tokenizer\n        self.tokens = self.tokenizer.encode(self.text, out_type=int)\n        self.context_length = context_length\n\n    def __len__(self):\n        return len(self.tokens) - self.context_length\n\n    def __getitem__(self, idx):\n        input_ids = self.tokens[idx : idx + self.context_length]\n        target_ids = self.tokens[idx + 1 : idx + self.context_length + 1]\n        return torch.tensor(input_ids), torch.tensor(target_ids)\n\n# Load tokenizer and dataset\nsp = spm.SentencePieceProcessor(model_file='malayalam_bpe.model')\ndataset = MalayalamDataset('/kaggle/input/datamalayalam/5000data', sp)\n\n# DataLoader for batching\ntrain_loader = DataLoader(dataset, batch_size=8, shuffle=True)\n\n# Test sample\nfor x, y in train_loader:\n    print(x.shape, y.shape)\n    break\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-03-26T08:51:32.836609Z","iopub.execute_input":"2025-03-26T08:51:32.836985Z","iopub.status.idle":"2025-03-26T08:51:34.119992Z","shell.execute_reply.started":"2025-03-26T08:51:32.836956Z","shell.execute_reply":"2025-03-26T08:51:34.119166Z"}},"outputs":[{"name":"stdout","text":"torch.Size([8, 256]) torch.Size([8, 256])\n","output_type":"stream"}],"execution_count":6},{"cell_type":"code","source":"import torch.nn as nn\nimport torch\n\nclass MiniGPT(nn.Module):\n    def __init__(self, vocab_size=5000, n_embd=256, n_layer=1, n_head=4, context_length=256):\n        super().__init__()\n\n        # ✅ Add an embedding layer to convert token indices into dense vectors\n        self.embedding = nn.Embedding(vocab_size, n_embd)\n\n        # Transformer Encoder\n        self.transformer = nn.TransformerEncoder(\n            nn.TransformerEncoderLayer(d_model=n_embd, nhead=n_head, dim_feedforward=4 * n_embd),\n            num_layers=n_layer\n        )\n\n        # Output layer (Language Model head)\n        self.lm_head = nn.Linear(n_embd, vocab_size)\n\n    def forward(self, x):\n        x = self.embedding(x)  # ✅ Convert token IDs to embeddings\n        x = self.transformer(x)\n        return self.lm_head(x)\n\n\n# Initialize model\nmodel = MiniGPT()\nprint(sum(p.numel() for p in model.parameters()), \"Total Parameters\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-03-26T08:51:45.098829Z","iopub.execute_input":"2025-03-26T08:51:45.099166Z","iopub.status.idle":"2025-03-26T08:51:45.134158Z","shell.execute_reply.started":"2025-03-26T08:51:45.099141Z","shell.execute_reply":"2025-03-26T08:51:45.133294Z"}},"outputs":[{"name":"stdout","text":"3354760 Total Parameters\n","output_type":"stream"},{"name":"stderr","text":"/usr/local/lib/python3.10/dist-packages/torch/nn/modules/transformer.py:379: UserWarning: enable_nested_tensor is True, but self.use_nested_tensor is False because encoder_layer.self_attn.batch_first was not True(use batch_first for better inference performance)\n  warnings.warn(\n","output_type":"stream"}],"execution_count":7},{"cell_type":"code","source":"import torch.optim as optim\n\ndevice = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\nmodel = MiniGPT().to(device)\noptimizer = optim.AdamW(model.parameters(), lr=3e-4)\ncriterion = nn.CrossEntropyLoss()\n\n# Training Loop\nfor epoch in range(1):  # Adjust epochs\n    for x, y in train_loader:\n        x, y = x.to(device), y.to(device)\n        optimizer.zero_grad()\n        logits = model(x)\n        loss = criterion(logits.view(-1, logits.size(-1)), y.view(-1))\n        loss.backward()\n        optimizer.step()\n    \n    print(f\"Epoch {epoch + 1}, Loss: {loss.item():.4f}\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-03-26T08:51:51.039787Z","iopub.execute_input":"2025-03-26T08:51:51.040150Z","iopub.status.idle":"2025-03-26T08:59:43.247130Z","shell.execute_reply.started":"2025-03-26T08:51:51.040122Z","shell.execute_reply":"2025-03-26T08:59:43.246342Z"}},"outputs":[{"name":"stdout","text":"Epoch 1, Loss: 3.9434\n","output_type":"stream"}],"execution_count":8},{"cell_type":"code","source":"import torch.nn.functional as F\n\ndef generate_text(model, tokenizer, start_text=\"മലയാളം\", max_len=100):\n    model.eval()\n    input_ids = torch.tensor(tokenizer.encode(start_text, out_type=int)).unsqueeze(0).to(device)\n    \n    for _ in range(max_len):\n        with torch.no_grad():\n            logits = model(input_ids)\n        next_token = torch.argmax(F.softmax(logits[:, -1, :], dim=-1), dim=-1)\n        input_ids = torch.cat([input_ids, next_token.unsqueeze(0)], dim=1)\n    \n    return tokenizer.decode(input_ids.squeeze(0).tolist())\n\nprint(generate_text(model, sp, \"മലയാളം\", max_len=50))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-03-26T08:59:43.248295Z","iopub.execute_input":"2025-03-26T08:59:43.248607Z","iopub.status.idle":"2025-03-26T08:59:43.421201Z","shell.execute_reply.started":"2025-03-26T08:59:43.248586Z","shell.execute_reply":"2025-03-26T08:59:43.420434Z"}},"outputs":[{"name":"stdout","text":"മലയാളം ഭാഷയിൽ \"അമ്മുടെ സംഘടനയുടെ കാലത്ത് ഈ പ്രദേശത്തെത്തുടർന്ന് 19836\" title=\"https://ml. ഈ പ്രദേശത്തെത്തുടർന്ന് 19836\" title=\"https://ml. ഈ പ്രദേശത്തെത്തുടർന്ന് 19836\" title=\"https://ml. ഈ പ്രദേശത്തെ\n","output_type":"stream"}],"execution_count":9},{"cell_type":"code","source":"","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"","metadata":{"trusted":true},"outputs":[],"execution_count":null}]}